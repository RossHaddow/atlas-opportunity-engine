{
  "name": "atlas-opportunity-engine",
  "version": "2.0.0",
  "private": true,
  "description": "Atlas personal opportunity engine",
  "scripts": {
    "start": "node server-build43.js",
    "test": "node --test",
    "check": "node --check server.js && node --check server-build43.js && node --check storage.js && node --check daily-focus.js && node --check public/daily-focus.js && node --test"
  },
  "engines": {
    "node": ">=22.5"
  }
}
