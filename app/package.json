{
  "name": "atlas-opportunity-engine",
  "version": "1.8.0",
  "private": true,
  "description": "Atlas personal opportunity engine",
  "scripts": {
    "start": "node server-build41.js",
    "test": "node --test",
    "check": "node --check server.js && node --check server-build41.js && node --check storage.js && node --check daily-focus.js && node --test"
  },
  "engines": {
    "node": ">=22.5"
  }
}
