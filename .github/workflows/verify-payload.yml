name: Verify Atlas payload

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify segmented Atlas payload
        run: bash scripts/verify-atlas.sh .
